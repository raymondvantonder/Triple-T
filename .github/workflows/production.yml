name: Triple-T Production Backend Deployment

on:
  push:
    branches: 
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - uses: ./deploy-action.yaml












